<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="3.5" DefaultTargets="CopyFiles" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
    
    <PropertyGroup>
        <SolutionDir>.</SolutionDir>
    </PropertyGroup>
    
    <ItemGroup>
        <GateFiles Include="$(SolutionDir)\Publish\ZipXmlGate\*.*" />
        <GateFiles Include="$(SolutionDir)\Publish\YahooCsvGate\*.*" />
    </ItemGroup>

    <Target Name="CopyFiles">
        <Copy
            SourceFiles="@(GateFiles)"
            DestinationFolder="."
        />
    </Target>
    
</Project>
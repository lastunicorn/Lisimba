<UserControl x:Class="DustInTheWind.Lisimba.Wpf.MainWindows.BirthdayViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:egg="clr-namespace:DustInTheWind.Lisimba.Egg.AddressBookModel;assembly=DustInTheWind.Lisimba.Egg"
             xmlns:mainWindows="clr-namespace:DustInTheWind.Lisimba.Wpf.MainWindows"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             d:DataContext="{d:DesignInstance egg:Date}">

    <UserControl.Resources>
        <mainWindows:DateFormatter x:Key="DateFormatter"/>
    </UserControl.Resources>

    <Grid>
        
        <TextBlock
            Name="BirthdayLabel"
            HorizontalAlignment="Stretch"
            Text="{Binding Converter={StaticResource DateFormatter}}"
            TextAlignment="Center"
            MouseDown="BirthdayLabel_MouseDown"/>

        <TextBox
            Name="BirthdayEditor"
            HorizontalAlignment="Stretch"
            TextAlignment="Center"
            Visibility="Collapsed"
            LostFocus="BirthdayEditor_LostFocus"/>
        
    </Grid>
    
</UserControl>
